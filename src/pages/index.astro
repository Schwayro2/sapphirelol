---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';
---

<!doctype html>
<html lang="en">
  <head>
    <BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" />
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
    <style is:global>
      :root {
        --primary: #6366f1;
        --primary-dark: #4f46e5;
        --text: #1f2937;
        --text-light: #6b7280;
        --bg: #ffffff;
        --card: #f9fafb;
        --border: #e5e7eb;
        --radius: 14px;
        --shadow: 0 8px 32px rgba(99,102,241,0.12);
        --plasma-gradient: linear-gradient(90deg, #ff41ca, #5f007f, #2335be, #c321a5, #2af5ff);
        --glow: 0 0 64px 0 #a5b4fc, 0 0 32px 8px #2af5ff60;
      }

      body {
        font-family: 'Inter', sans-serif;
        color: var(--text);
        background: var(--bg);
        line-height: 1.5;
        scroll-behavior: smooth;
      }
      main {
        max-width: 1000px;
        margin: auto;
        padding: 1.5rem;
      }
      .hero {
        display: flex;
        flex-direction: column;
        align-items: center;
        text-align: center;
        padding: 4.5rem 1rem 3.5rem;
        opacity: 0;
        transform: translateY(20px);
        transition: all 0.8s cubic-bezier(.46,.03,.52,.96);
        position: relative;
        min-height: 430px;
        background: transparent;
      }
      .hero.visible {
        opacity: 1;
        transform: translateY(0);
      }
      .hero h1 {
        font-size: 2.8rem;
        font-weight: 700;
        background: var(--plasma-gradient);
        -webkit-background-clip: text;
        background-clip: text;
        color: transparent;
        margin-bottom: 0.7rem;
        letter-spacing: -1.5px;
      }
      .hero p {
        color: var(--text-light);
        margin-bottom: 1.7rem;
        font-size: 1.25rem;
        max-width: 560px;
        margin-left: auto;
        margin-right: auto;
      }
      .btn-group {
        display: flex;
        gap: 0.9rem;
        flex-wrap: wrap;
        justify-content: center;
        margin-bottom: 2.3rem;
      }
      .btn {
        padding: 0.75rem 1.5rem;
        border-radius: var(--radius);
        font-weight: 600;
        text-decoration: none;
        transition: 0.3s;
        font-size: 1.08rem;
        box-shadow: 0 2px 8px #b2b9ff18;
      }
      .btn-primary {
        background: var(--primary);
        color: white;
      }
      .btn-primary:hover { background: var(--primary-dark); }
      .btn:not(.btn-primary) {
        border: 1px solid var(--border);
        color: var(--primary);
        background: transparent;
      }
      .btn:not(.btn-primary):hover {
        background: #f0f1fa;
      }
      .hero-imgui-center {
        width: 520px;
        max-width: 95vw;
        margin: 0 auto;
        margin-top: 0;
        margin-bottom: 0;
        border-radius: 18px;
        box-shadow: var(--glow), 0 6px 36px #6366f122;
        filter: drop-shadow(0 12px 36px #8eb5fd30);
        will-change: transform, filter;
        transition: transform 0.8s cubic-bezier(.46,.03,.52,.96), box-shadow 0.4s;
        position: relative;
        z-index: 2;
        background: transparent;
        animation: floatImguiCenter 4s ease-in-out infinite alternate;
      }
      @keyframes floatImguiCenter {
        0% { transform: scale(1) translateY(0); }
        100% { transform: scale(1.03) translateY(-22px); }
      }
      @media (max-width: 700px) {
        .hero-imgui-center {
          width: 99vw;
          min-width: 0;
          max-width: 99vw;
        }
        .hero h1 { font-size: 2.1rem; }
        .hero { padding: 2.5rem 0.5rem 2.2rem; }
      }
    </style>
  </head>
  <body>
    <Header />
    <main>
      <section class="hero animate-on-scroll">
        <h1>Next-Level Game Enhancements</h1>
        <p>
          Discover cutting-edge tools engineered for elite gamers who demand stealth, precision, and top-tier performance.
        </p>
        <div class="btn-group">
          <a href="#products" class="btn btn-primary">Explore Products</a>
          <a href="#features" class="btn">How It Works</a>
        </div>
        <img
          src="https://files.catbox.moe/s0z1tu.png"
          alt="ImGui Menu Screenshot"
          class="hero-imgui-center"
          id="imguiMenu"
        />
      </section>

      <section id="products" class="animate-on-scroll">
        <h2 class="section-title">Premium Products</h2>
        <div class="grid products-grid">
          <div class="card">
            <h3>BO6 / Warzone External</h3>
            <ul style="margin-top: 0.5rem; padding-left: 1.25rem; font-size: 0.95rem;">
              <li>Aimbot with smoothing</li>
              <li>ESP (Players, Loot, Vehicles)</li>
              <li>Config support</li>
              <li>Stream-proof</li>
            </ul>
          </div>
          <div class="card">
            <h3>Permanent HWID Spoofer</h3>
            <ul style="margin-top: 0.5rem; padding-left: 1.25rem; font-size: 0.95rem;">
              <li>UEFI kernel-based</li>
              <li>Spoofs serials & SMBIOS</li>
              <li>Stealth boot cleanup</li>
              <li>Persistent across reboots</li>
            </ul>
          </div>
          <div class="card">
            <h3>Temporary HWID Spoofer</h3>
            <ul style="margin-top: 0.5rem; padding-left: 1.25rem; font-size: 0.95rem;">
              <li>Instant spoof on launch</li>
              <li>Resets on reboot</li>
              <li>Compatible with most AC</li>
            </ul>
          </div>
        </div>
      </section>
      <section id="features" class="animate-on-scroll">
        <h2 class="section-title">Why Choose Us?</h2>
        <div class="grid features-grid">
          <div class="card">
            <h3><i class="fas fa-shield-alt"></i> Undetected!</h3>
            <p>Our software has its own stealthty methods and has consistent backend updates to avoid detection by modern anti-cheat systems.</p>
          </div>
          <div class="card">
            <h3><i class="fas fa-bolt"></i> Good Performance</h3>
            <p>Built for speed and stability, our solutions are lightweight and optimized to maintain flawless performance without game interruptions.</p>
          </div>
          <div class="card">
            <h3><i class="fas fa-sync-alt"></i> Always Updated</h3>
            <p>Stay ahead with tools that are updated daily, ensuring compatibility with every patch, update, and meta change in your favorite games.</p>
          </div>
        </div>
      </section>
      <section class="testimonials animate-on-scroll">
        <h2 class="section-title">User Feedback</h2>
        <div class="grid testimonials-grid">
          <div class="card"> <p>"I've been using the Fortnite Suite across multiple seasons—still undetected, insanely responsive, and always ahead of the curve."</p> <strong>- ProPlayer123</strong> </div>
          <div class="card"> <p>"Went from Diamond to Immortal in just two weeks thanks to the seamless aim assist and smart radar features. Unreal advantage!"</p> <strong>- CompetitiveGamer</strong> </div>
          <div class="card"> <p>"Support was lightning-fast and extremely helpful. Setup was smooth and I felt confident within minutes of installing."</p> <strong>- StreamerXYZ</strong> </div>
        </div>
      </section>
    </main>
    <Footer />

    <script>
      // Animate on scroll for cards/sections
      document.addEventListener('DOMContentLoaded', () => {
        const animatedEls = document.querySelectorAll('.animate-on-scroll, .card');
        const observer = new window.IntersectionObserver(entries => {
          entries.forEach(entry => {
            if (entry.isIntersecting) {
              entry.target.classList.add('visible');
            } else {
              entry.target.classList.remove('visible');
            }
          });
        }, { threshold: 0.1 });
        animatedEls.forEach(el => observer.observe(el));

        // Parallax for ImGui menu image in hero
        const imguiMenu = document.getElementById('imguiMenu');
        if (imguiMenu) {
          window.addEventListener('scroll', () => {
            const scrolled = window.scrollY;
            imguiMenu.style.transform =
              `scale(1.06) translateY(${Math.min(scrolled / 9, 40)}px)`;
          });
        }
      });
    </script>
  </body>
</html>
